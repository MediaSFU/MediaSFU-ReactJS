declare let QnHDCons: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let sdCons: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let hdCons: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let fhdCons: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let qhdCons: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let QnHDConsPort: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let sdConsPort: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let hdConsPort: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let fhdConsPort: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let qhdConsPort: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let QnHDConsNeu: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let sdConsNeu: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let hdConsNeu: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let fhdConsNeu: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let qhdConsNeu: {
    width: {
        ideal: number;
    };
    height: {
        ideal: number;
    };
};
declare let QnHDFrameRate: number;
declare let sdFrameRate: number;
declare let hdFrameRate: number;
declare let fhdFrameRate: number;
declare let qhdFrameRate: number;
declare let screenFrameRate: number;
/**
 * Exports various video capture constraints and frame rates.
 *
 * @exports
 * - `QnHDCons`: Constraints for Quarter HD resolution.
 * - `sdCons`: Constraints for Standard Definition resolution.
 * - `hdCons`: Constraints for High Definition resolution.
 * - `fhdCons`: Constraints for Full HD resolution.
 * - `qhdCons`: Constraints for Quad HD resolution.
 * - `QnHDConsPort`: Constraints for Quarter HD resolution (portrait mode).
 * - `sdConsPort`: Constraints for Standard Definition resolution (portrait mode).
 * - `hdConsPort`: Constraints for High Definition resolution (portrait mode).
 * - `fhdConsPort`: Constraints for Full HD resolution (portrait mode).
 * - `qhdConsPort`: Constraints for Quad HD resolution (portrait mode).
 * - `QnHDConsNeu`: Constraints for Quarter HD resolution (neutral mode).
 * - `sdConsNeu`: Constraints for Standard Definition resolution (neutral mode).
 * - `hdConsNeu`: Constraints for High Definition resolution (neutral mode).
 * - `fhdConsNeu`: Constraints for Full HD resolution (neutral mode).
 * - `qhdConsNeu`: Constraints for Quad HD resolution (neutral mode).
 * - `QnHDFrameRate`: Frame rate for Quarter HD resolution.
 * - `sdFrameRate`: Frame rate for Standard Definition resolution.
 * - `hdFrameRate`: Frame rate for High Definition resolution.
 * - `fhdFrameRate`: Frame rate for Full HD resolution.
 * - `qhdFrameRate`: Frame rate for Quad HD resolution.
 * - `screenFrameRate`: Frame rate for screen capture.
 *
 */
export { QnHDCons, sdCons, hdCons, fhdCons, qhdCons, QnHDConsPort, sdConsPort, hdConsPort, fhdConsPort, qhdConsPort, QnHDConsNeu, sdConsNeu, hdConsNeu, fhdConsNeu, qhdConsNeu, QnHDFrameRate, sdFrameRate, hdFrameRate, fhdFrameRate, qhdFrameRate, screenFrameRate, };
//# sourceMappingURL=videoCaptureConstraints.d.ts.map